### 漏洞
- 偶现 | 抓帧时崩溃
  - System.Runtime.InteropServices.ExternalException (0x80004005): GDI+ 中发生一般性错误
    在 System.Drawing.Image.Save(String filename, ImageCodecInfo encoder, EncoderParameters encoderParams)
---
- 偶现 | 观摩室接收两路，发送端关闭一路后，导致观摩室端崩溃
- 必现 | 当已选设备拔除后，再次入会后不应选中不存在的设备
- 必现 | 当app第一次打开，用户未操作过界面，导致接听响铃没有声音
  - cef exception : play() failed because the user didn't interact with the document first
- 偶现 | （win7虚拟机）初始化共享窗口管理器时，检测到异常id对象，getName异常
- 偶现 | 客户机发现初始化显示器管理器时，程序发生异常（SDK和应用层），getId异常
- 必现 | SDK x86下，接收会控消息异常，state全为true（已上报给vidyo并确认问题存在）
---
- 偶现 | 出现两个相同参会者
- 偶现 | 当某参会者信息未触发移除时，此参会人再次进入会中导致触发键相同异常，引发软件崩溃

-------------------------------------------------------------------------------------------------------------
### 功能
- 视频页显示网络质量
- 显示Tango连接状态
- 显示禁用本地摄像头标识
- 实现Tango Guest
- 其他模块：
  - 档案模块中的查询录播文件功能的服务器地址目前为硬编码，需要
  - 档案模块中的生成邀请信息需要改为统一版本（已实现了对话框作为复用组件）
-------------------------------------------------------------------------------------------------------------
### 优化
- 调整版本号字体 ok
- 显示被挂断消息提示 ok
- 视频页
  - 底部显示按钮文字
  - 顶部增加标题栏
-------------------------------------------------------------------------------------------------------------